name = "tele-ngo-api"
main = "src/index.ts"
compatibility_date = "2025-07-18"
compatibility_flags = ["nodejs_compat"]

[vars]
PAN_PROVIDER = "mock"
ALLOWED_ORIGIN = "http://localhost:5173"

# Secrets: `wrangler secret put <NAME>` in production, apps/api/.dev.vars locally. See README.

[observability]
enabled = true
